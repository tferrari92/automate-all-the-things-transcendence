# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: alb-production-cluster-issuer
# spec:
#   acme:
#     # Let’s Encrypt has a strict rate limit on certificates issued, so we use the Staging Let’s Encrypt server for testing, and then we switch to the Production Let’s Encrypt server.
#     # Production Let’s Encrypt server
#     server: https://acme-v02.api.letsencrypt.org/directory
#     email: {{ .Values.email }}
#     privateKeySecretRef:
#       name: alb-production-cluster-issuer # Name of the key that will be generated by the Issuer for the Cert
#     solvers:
#       - selector: {}
#         http01:
#           ingress:
#             class: alb


